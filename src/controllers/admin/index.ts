import { default as verification } from "./verification.controller";
import { default as user } from "./user.controller";
import { default as item } from "./item.controller";
import { default as category } from "./category.controller";
import { default as feedback } from "./feedback.controller";
import { default as profile } from "./profile.controller";

export default {
    verification,
    user,
    item,
    category,
    feedback,
    profile,
};
